<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.example.sample.service.impl.CoopMapper">
	
	
	<select id="getCoopList" resultType="egovframework.example.sample.service.CoopVO">
	
		select * from coop
	
	</select>
	
	
	<insert id="insertCoop">
		INSERT INTO coop (id, latitude, longitude, date, time)
		SELECT NEXTVAL('coop_seq'), latitude, longitude,
       	TO_CHAR(CURRENT_TIMESTAMP, 'yyyy-mm-dd'), TO_CHAR(CURRENT_TIMESTAMP, 'hh24:mi:ss')
		FROM temp
	</insert>
	
	
</mapper>