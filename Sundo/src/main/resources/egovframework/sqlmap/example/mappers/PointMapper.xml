<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.example.sample.service.impl.PointMapper">


	<insert id="insertPoint">
		
		INSERT INTO points (gid, lon, lat, date, time, geom, car_num, noise, rpm)
		VALUES (
		    NEXTVAL('gid_seq'),          
		    #{longitude},          
		    #{latitude},          
		    TO_CHAR(CURRENT_TIMESTAMP + INTERVAL '1 day', 'yyyy-mm-dd'),
		    TO_CHAR(CURRENT_TIMESTAMP, 'hh24:mi:ss')::time,
		    ST_SetSRID(ST_MakePoint(CAST(#{longitude} AS DOUBLE PRECISION), CAST(#{latitude} AS DOUBLE PRECISION)), 4326), 
		    '114하6585',     
		    90,        
		    1500      
		)

	</insert>


</mapper>